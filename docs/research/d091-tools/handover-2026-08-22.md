# Handover D-091 — 22 août 2026

## État Git

- branche : `codex/curated-solutions-quality-engine` ;
- base : `cca1459a` (`main = origin/main` au démarrage) ;
- PR de fondation : [#178](https://github.com/teamdemaa/demaa.fr/pull/178) ;
- commit technique contrôlé avant cette mise à jour documentaire : `5027af58` ;
- branche poussée et CI verte ; aucun merge, déploiement Production ou
  changement de pointeur Firebase ;
- la Production conserve intégralement le registre actif précédent.

## Ce que le lot prépare

- sélection Outils variable, déterminée par un seuil de preuve et non par un
  quota ;
- distinction entre catalogue complet des 115 systèmes et périmètre éditorial
  des cinq pilotes ;
- lien bloquant entre les outils réellement retenus et la couverture des
  besoins déclarés dans le manifeste de recherche ;
- ordre Firebase obligatoirement conforme au classement relu dans le manifeste
  de recherche ;
- gate Outils sur ressources/placements publiés, sources officielles,
  contraintes, justification, ordre et stabilité des identifiants ;
- gate Fournisseurs/Réseaux autorisant une rubrique vide mais refusant toute
  entrée cachée, brouillon ou non prouvée ;
- provenance explicite des sélections Financement (`system` ou `default`) et
  Aides (`system`, `sector` ou `default`) sans changement du rendu ;
- JSON-LD Outils sans troncature arbitraire ;
- limite Firebase Outils portée à 50 comme garde technique uniquement, avec
  preuve qu'une sélection supérieure à dix est acceptée ; les autres sections
  conservent leur garde technique historique ;
- manifeste de recherche et revue des cinq pilotes ;
- trois preuves officielles manquantes ajoutées au répertoire canonique :
  Vercel, Lefebvre Dalloz et Sage Batigest Connect.

## État des données

Le manifeste `pilot-selections.v2.json` contient 50 occurrences candidates sur
cinq systèmes. Elles ont toutes une source HTTPS et une date de revue récente,
mais elles restent des **pools**, jamais des listes finales.

Aucune révision Firebase candidate n'a été créée. Aucun candidat n'a été rendu
visible.

Le rapport `npm run report:d091:baseline` mesure :

- Outils : 1 à 7 par système dans le snapshot local, moyenne 3,39 ;
- Fournisseurs publiés : 3 systèmes sur 115 ;
- Financement : 95 systèmes sur 115 utilisent le fallback générique ;
- Aides : 115 sélections explicites, encore dépourvues d'un contrat de
  fraîcheur runtime ;
- Réseaux publiés : 0.

Ces volumes sont des diagnostics, pas des objectifs à remplir.

## Contrôles exécutés

- `npm run check` : vert ;
- 255 fichiers de tests, 1 430 tests : verts ;
- TypeScript et ESLint strict : verts ;
- validations Académie et données : vertes ;
- `npm run build` : vert, 433 pages générées ;
- `git diff --check` : vert au dernier contrôle avant handover.

## Suite obligatoire avant toute Preview de données

1. conduire la seconde revue métier des cinq pools et retirer les candidats
   redondants ou insuffisamment distincts ;
2. valider pour chaque élément retenu l'usage, la contrainte et la preuve
   officielle qui justifient son placement dans ce métier ;
3. traiter séparément les fallbacks Financement, la fraîcheur des Aides, les
   Fournisseurs et les Réseaux — une rubrique vide reste valide ;
4. figer un snapshot de l'active et construire une révision candidate complète
   des 115 systèmes, sans changer le pointeur ;
5. lancer `npm run audit:d091:pilot -- <candidate.json> [active.json] [research.json]` ;
6. recetter API, interface, pages publiques, HTML et JSON-LD sur Preview ;
7. généraliser seulement après validation du pilote ;
8. soumettre tout déplacement du pointeur à un GO PROD explicite avec rollback.

## Interdictions maintenues

- aucun objectif uniforme de dix outils ;
- aucun badge, score ou nouveau composant sur les cartes ;
- aucun import automatique des `toolRefs` ;
- aucun mélange des Services avec Outils ou Fournisseurs ;
- aucune activation anglaise ;
- aucune publication depuis un pool de recherche.

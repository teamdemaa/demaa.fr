# Handover — 20 août 2026

Session de travail avec Claude Code, du 18 au 20 août 2026. Écrit pour reprise
par un autre agent (Codex) sans perte de contexte.

## État général au moment du handover

- `main` à jour avec `origin/main` (`6fd6dcd`), déployé en production, build
  et checks verts.
- **Aucune PR ouverte.**
- Working tree propre (aucun changement local non commité).
- `npm run check` (lint strict `--max-warnings=0`, `tsc --noEmit`, tests,
  validations académie/data) passe intégralement sur `main`.

## Ce qui a été livré cette session (vérifié en prod)

- Réactivation d'Opportunités (page publique `/opportunites` + onglet
  applicatif en 4ᵉ position) et clarification Académie vs Ressources (le
  libellé "Académie" a été conservé, pas renommé).
- Activation de Stratégie (Chiffres l'était déjà) — `COMPANY_STRATEGY_VISIBLE`
  passé à `true`, constante centralisée dans `action-plan-app-context.ts`.
- Correction d'un bug d'accordéon : un pilier Stratégie déjà ouvert ne se
  refermait pas au clic.
- Contenu des 12 questions Stratégie affiné : `alignment_3` remplace la
  question de délégation par une question de contraintes (temps/argent/
  énergie) — décision explicite pour garder le framework générique, non lié
  à la promesse marketing Demaa. `positioning_3` et `offer_3` enrichies
  après relecture croisée.
- Suppression d'un composant mort confirmé (`HistoricalOperationalSystemCopyRequestModal.tsx`,
  462 lignes, aucun import réel, seulement pinné par des tests).
- **Sécurité admin** : remplacement du secret partagé statique
  (`x-demaa-admin-secret`) par une session Google/email réutilisant le
  système d'authentification client existant. Voir `docs/coaching-admin-access.md`
  pour le détail. Nouvelle page `/admin/demandes` : récap des demandes déjà
  centralisées dans `lead_requests` (rappel service, profil prestataire,
  intérêt opportunité, mise en relation coaching/comptable, kit système) —
  auparavant visibles uniquement via Slack/email, jamais en admin.
- Ménage : 60 branches locales + 15 branches distantes supprimées (toutes
  déjà mergées dans `main`, aucun risque).

## Mise à jour de clôture — 20 août 2026

- `DEMAA_ADMIN_EMAILS` est configurée dans Vercel Preview et Production.
- La session Google autorisée a ouvert avec succès `/admin/coaching`,
  `/admin/opportunites` et `/admin/demandes` en Production.
- Les anciens secrets `OPPORTUNITIES_ADMIN_SECRET` et
  `COACHING_ADMIN_SECRET` ont été retirés de Vercel Production.
- La bêta anglaise reste volontairement absente de Production. Sa reprise est
  documentée dans D-085 et l'ADR 0014, avec parité fonctionnelle et mêmes
  métiers de petites entreprises que le français comme gates obligatoires.

## Historique — blocage initial désormais fermé

**1. Ajouter `DEMAA_ADMIN_EMAILS` dans Vercel (Production → Environment
Variables).** Cette variable n'existait pas au moment du handover. Tant qu'elle
n'était pas définie,
`/admin/opportunites`, `/admin/coaching` et `/admin/demandes` sont
inaccessibles à tout le monde, y compris au fondateur. Valeur attendue :
liste d'emails séparés par des virgules (ex. `hi.teamdemaa@gmail.com`).
C'était un choix volontaire (fermé par défaut plutôt qu'ouvert par erreur),
pas un bug. L'action est désormais réalisée et recettée.

## À faire — décisions en attente, non bloquant

**2. Studio vidéo** (`studio/academy-video-prototype/` ~740 Mo,
`studio/cookie-video-01/` et `-02/` ~70 Mo) — garder ou supprimer, décision
mise en pause par le fondateur le 19/08. Ce sont de vrais projets créatifs
(storyboards, plans vidéo, assets approuvés), pas des restes ; à ne pas
supprimer sans confirmation explicite. Note technique : même supprimés du
répertoire de travail, ces fichiers resteraient dans l'historique Git (le
dépôt fait ~1 Go) tant qu'aucune réécriture d'historique n'est faite — une
opération distincte, plus délicate (force-push, re-clone nécessaire pour
tout collaborateur), à ne faire que sur demande explicite séparée.

**3. Dix branches distantes antérieures à cette session** — identifiées lors
de l'audit comme "vrai travail non mergé, à trier manuellement" (contrairement
aux 60+15 supprimées, celles-ci ont du contenu qui n'est pas simplement une
divergence historique) : `academie-safe-zone-staging`,
`academie-thumbnail-contrast-staging`, `audit-hardening-and-tests`,
`conception-montage-video-service`, `d061-process-unification-staging`,
`d061-visual-qa-fix`, `d061-workbook-v2-pilots-staging`, `kit-btp-conversion`,
`resources-callback-integration`, `systemes-release-staging`. Aucun risque à
les laisser en l'état (branches invisibles tant qu'on ne les checkout pas,
n'affectent ni `main` ni la prod) — mais elles ne se trieront pas seules.

**4. Anciens secrets `OPPORTUNITIES_ADMIN_SECRET` et `COACHING_ADMIN_SECRET`**
retirés de Vercel Production après validation réelle de la nouvelle session
admin. Ce point est fermé.

## Basse priorité — repéré par l'audit sécurité du 18/08, jamais traité

- Token de session "kit analytics" (`src/lib/kit-analytics-auth.server.ts`) :
  HMAC statique, non expirant individuellement (cookie 12h, pas de révocation
  ciblée possible sans changer le mot de passe maître).
- CSP (`src/lib/content-security-policy.ts`) autorise `unsafe-inline` sur
  `script-src`/`style-src` en production — compromis courant pour
  Analytics/GTM, pas anormal, mais réduit la protection CSP si une faille XSS
  apparaissait ailleurs.
- Dépendances mineures en retard : `typescript` (5.9→7.0, devDependency),
  `@types/node`, `google-auth-library`, `vitest` — **0 vulnérabilité npm**,
  aucune urgence.

## Où chercher le contexte

- `docs/handover-internationalization-paused-2026-08-21.md` : état figé de la
  pile anglaise, PR ordonnées, gates ouvertes et procédure sûre de reprise ;
- `docs/central-backlog.md` : journal de décisions vivant, mis à jour tout
  au long de cette session (chercher les entrées datées 18-19-20 août 2026).
- `docs/coaching-admin-access.md` : procédure d'accès admin à jour.
- `docs/decisions/0013-company-strategy-cycles.md` : ADR original de
  Chiffres/Stratégie (non modifié — le changement de `alignment_3` est
  documenté dans central-backlog.md, pas dans l'ADR, pour préserver le
  registre historique).
